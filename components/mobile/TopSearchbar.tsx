import classNames from "classnames/bind";
import style from "./topSearchbar.module.scss";
const cx = classNames.bind(style);

import React from "react";

const TopSearchbar = () => {
  return <div className={cx("test")}>hello</div>;
};

export default TopSearchbar;
